experiment:
  name: sp500_50_panel_beta_cvae_latent8_hidden64
  dataset: sp500_50_panel
  seed: 0
data:
  n_samples: 89
  n_timesteps: 60
  data_dim: 50
  condition_dim: 2
  transform: id
  inverse_transform: id
  condition_feature: spy_vix_level
  params:
    processed_subdir: sp500_50_panel
    condition_mode: spy_vix_level
model:
  objective: beta_cvae
  encoder: conditional_residual_lstm
  decoder: conditional_residual_lstm
  conditioner: identity
  prior: real_nvp
  data_dim: 50
  data_length: 60
  latent_dim: 8
  latent_length: 60
  beta: 0.04
  encoder_hidden_dim: 64
  encoder_num_layers: 2
  decoder_hidden_dim: 64
  decoder_num_layers: 2
  prior_num_flows: 3
  prior_hidden_dim: 128
training:
  optimizer: adam
  learning_rate: 0.001
  train_batch_size: 64
  eval_batch_size: 64
  epochs: 50
  steps_saving: 10
  steps_predict: 10
